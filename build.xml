<project name="google-sitebricks" default="make">
    <description>google-sitebricks: a ridiculously simple, statically-typed, restful web framework</description>

    <property file="build.properties"/>

    <path id="google-sitebricks.classpath">
        <fileset dir="${lib}">
            <include name="*.jar"/>
        </fileset>
    </path>

    <target name="clean" description="Cleans up the build artifacts and temporary directories">
        <delete dir="${build}"/>
    </target>


    <!-- setup testng -->
    <taskdef name="testng" classname="org.testng.TestNGAntTask">
        <classpath refid="google-sitebricks.classpath"/>
    </taskdef>

    <target name="init">
        <mkdir dir="${core-classes}"/>
        <mkdir dir="${test-classes}"/>
        <mkdir dir="${acceptance-test-classes}"/>
        <mkdir dir="${dir.distribute}"/>
    </target>

    <target name="make" depends="init" description="Compiles and tests google-sitebricks">
        <javac sourcepath="" srcdir="${src}" destdir="${core-classes}" debug="on">
            <include name="**/*.java"/>
            <exclude name="com/google/warp/example/**"/>

            <classpath refid="google-sitebricks.classpath"/>
        </javac>

        <!-- copy non binary resources to output classpath -->
        <copy todir="${core-classes}">
            <fileset dir="${src}">
                <include name="**/*.xml"/>
                <include name="**/*.html"/>
                <include name="**/*.xhtml"/>
                <exclude name="log4j.properties"/>
                <include name="**/*.properties"/>
                <include name="**/*.js"/>

                <exclude name="com/google/warp/example/**"/>
            </fileset>
        </copy>
        <copy todir="${test-classes}">
            <fileset dir="${test}">
                <include name="**/*.xml"/>
                <include name="**/*.html"/>
                <include name="**/*.xhtml"/>
                <exclude name="log4j.properties"/>
                <include name="**/*.properties"/>
                <include name="**/*.js"/>

                <exclude name="com/google/warp/example/**"/>
            </fileset>
        </copy>


        <javac sourcepath="" srcdir="${test}" destdir="${test-classes}" debug="on">
            <include name="**/*.java"/>
            <classpath>
                <path refid="google-sitebricks.classpath"/>
                <pathelement location="${core-classes}"/>
            </classpath>
        </javac>


        <!-- run them with testng
        <testng haltonfailure="true" verbose="2" suitename="google-sitebricks unit tests">
            <classpath>
                <path refid="google-sitebricks.classpath"/>

                <pathelement location="${core-classes}"/>
                <pathelement location="${test-classes}"/>
            </classpath>

            <classfileset dir="${test-classes}" includes="**/sitebricks/**/*Test.class"/>
        </testng>
        -->

    </target>

    <target name="jar" depends="clean,make" description="Assembles a distributable jar and zip (for direct use in webapps)">
        <taskdef name="jarjar" classname="com.tonicsystems.jarjar.JarJarTask" classpath="${lib}/jarjar-1.0rc7.jar"/>

        <jarjar jarfile="${build}/jartmp.jar">

            <!--google-sitebricks classes-->
            <fileset dir="${core-classes}">
                <!--<exclude name="com/google/warp/sitebricks/example/**"/>     -->
                <exclude name="META-INF/**"/>
            </fileset>

            <!--dependencies-->
            <zipfileset src="${lib}/asm.jar">
                <exclude name="META-INF/*"/>
            </zipfileset>
            <zipfileset src="${lib}/commons-io.jar">
                <exclude name="META-INF/*"/>
            </zipfileset>
            <zipfileset src="${lib}/dom4j-1.6.1.jar">
                <exclude name="META-INF/*"/>
            </zipfileset>
            <zipfileset src="${lib}/jaxen-1.1-beta-7.jar">
                <exclude name="META-INF/*"/>
            </zipfileset>
            <zipfileset src="${lib}/mvel-2.0-SNAPSHOT.jar">
                <exclude name="META-INF/*"/>
            </zipfileset>
            <zipfileset src="${lib}/commons-httpclient-3.1.jar">
                <exclude name="META-INF/*"/>
            </zipfileset>

             <!--repackage into google-sitebricks namespace-->
            <keep pattern="com.googlep.**"/>
            <rule pattern="org.objectweb.asm.**" result="com.googleebricksckscks@1"/>
            <rule pattern="org.mvel.**" result="com.googleebricksckscks.@1"/>
            <rule pattern="org.dom4j.**" result="com.googlegetsj.@1"/>
            <rule pattern="org.apache.commons.io.**" result="com.googlegets1"/>
            <rule pattern="org.jaxen.**" result="com.googlegetsn.@1"/>
            <rule pattern="org.apache.commons.httpclient.**" result="com.googlegetsclient.@1"/>
        </jarjar>

        <!-- horrendous workaround needed coz jarjar does not put in directory entries -->
        <unzip src="${build}/jartmp.jar" dest="${jartmp}"/>

        <!-- rejar with regular jar to generated jar entries -->
        <jar jarfile="${dir.distribute}/google-sitebricks-${version}.jar" index="true">
            <fileset dir="${build}/jartmp"/>
        </jar>

      
        <!-- run them with testng
        <testng haltonfailure="true" verbose="2" suitename="google-sitebricks unit tests on jar">
            <classpath>
                <path refid="google-sitebricks.classpath"/>

                <pathelement location="${test-classes}"/>
                <pathelement location="${dir.distribute}/google-sitebricks-${version}.jar"/>
            </classpath>

            <classfileset dir="${build}" includes="**/sitebricks/**/*Test.class"/>
        </testng>
        -->

        <jar basedir="${src}" destfile="${dir.distribute}/google-sitebricks-src-${version}.jar"/>


        <!-- make a staging dir for distribution -->
        <mkdir dir="${staging}/WEB-INF/lib"/>
        <copy todir="${staging}/WEB-INF/lib">
            <fileset dir="${dir.distribute}" includes="*.jar" excludes="google-sitebricks-src-*.jar"/>
            <fileset dir="${lib}">
                <include name="google-servlet-*.jar"/>
                <include name="guice-*.jar"/>
            </fileset>
        </copy>
        <copy todir="${staging}/WEB-INF">
            <fileset dir="${dir.resources}">
                <include name="web.xml"/>
            </fileset>
        </copy>
        <copy todir="${staging}">
            <fileset dir="${dir.resources}">
                <include name="*.html"/>
            </fileset>
        </copy>

        <zip basedir="${staging}" destfile="${dir.distribute}/google-sitebricks-full-${version}.zip"/>
    </target>

    <target name="acceptance" description="Runs acceptance tests using Jetty to verify widget rendering">
        <javac sourcepath="" srcdir="${acceptance-test}" destdir="${acceptance-test-classes}" debug="on">
            <include name="**/*.java"/>
            <classpath>
                <path refid="google-sitebricks.classpath"/>
                <!--<pathelement location="${core-classes}"/>-->

                <fileset dir="${dir.distribute}" includes="google-sitebricks-${version}.jar"/>
                <fileset dir="${lib}/webdriver">
                    <include name="*.jar"/>
                </fileset>
                <fileset dir="${lib}/jetty">
                    <include name="*.jar"/>
                </fileset>
                <pathelement location="${test-classes}"/>
            </classpath>
        </javac>

        <!-- run acceptance tests against library jar -->
        <testng haltonfailure="true" verbose="2" suitename="google-sitebricks acceptance tests">
            <classpath>
                <fileset dir="${lib}">
                    <include name="guice-1.0.jar"/>
                    <include name="google-servlet*.jar"/>
                </fileset>
                <fileset dir="${lib}/webdriver">
                    <include name="*.jar"/>
                </fileset>
                <fileset dir="${lib}/jetty">
                    <include name="*.jar"/>
                </fileset>
                <fileset dir="${dir.distribute}" includes="google-sitebricks-${version}.jar"/>
                <pathelement location="${acceptance-test-classes}"/>
            </classpath>

            <classfileset dir="${acceptance-test-classes}" includes="**/*AcceptanceTest.class"/>
        </testng>
    </target>

</project>
